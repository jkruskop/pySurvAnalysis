name: TestProject
question: ''
defaults:
  experiment_type: standard_lifespan
  global:
    time_unit: days
    time_label: Age (days)
    assume_censored: true
  factors:
    Sex:
    - Male
    - Female
    Density:
    - 20x
    - 40x
scripts:
- name: batch
  notes: Created with the project, and what a Batch Run runs here unless another script
    is designated. Analyses every member, renders their curated publication figures,
    then builds the project report. Edit or replace it in the Script Editor — a project
    with no script here cannot be run from the Project card or a Batch Run.
  steps:
  - action: run_in_experiments
    script: Standard analysis
  - action: render_publication_figures
  - action: project_report
experiment_scripts: []
